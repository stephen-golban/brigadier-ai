[package]
name = "claude-wire"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Wire-level serde types for the Claude Code CLI stream-json stdio protocol"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
