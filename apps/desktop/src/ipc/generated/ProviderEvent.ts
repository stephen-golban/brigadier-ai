// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ApprovalDecision } from "./ApprovalDecision";
import type { ApprovalRequest } from "./ApprovalRequest";
import type { Decider } from "./Decider";
import type { FileChange } from "./FileChange";
import type { ItemStatus } from "./ItemStatus";
import type { NoticeLevel } from "./NoticeLevel";
import type { ProviderError } from "./ProviderError";
import type { QuotaSnapshot } from "./QuotaSnapshot";
import type { Role } from "./Role";
import type { TokenUsage } from "./TokenUsage";
import type { TurnStatus } from "./TurnStatus";

/**
 * Everything a provider session reports, in one vocabulary for every CLI.
 *
 * Streaming text arrives as `*Delta` events keyed by `itemId`; the matching final event
 * (`message`, `reasoning`) carries the complete text and replaces whatever the deltas built.
 */
export type ProviderEvent = { "type": "sessionStarted", nativeId: string, model: string | null, cwd: string | null, cliVersion: string | null, } | { "type": "turnStarted", turnId: string | null, } | { "type": "messageDelta", itemId: string, text: string, } | { "type": "message", itemId: string, role: Role, text: string, } | { "type": "reasoningDelta", itemId: string, text: string, } | { "type": "reasoning", itemId: string, text: string, } | { "type": "toolCall", itemId: string, name: string, 
/**
 * Tool input as JSON text, once complete.
 */
input: string | null, status: ItemStatus, 
/**
 * Result text (truncated), once finished.
 */
output: string | null, } | { "type": "command", itemId: string, command: string, cwd: string | null, status: ItemStatus, exitCode: number | null, 
/**
 * Combined output (truncated), once finished.
 */
output: string | null, durationMs: number | null, } | { "type": "commandOutputDelta", itemId: string, text: string, } | { "type": "fileChanges", itemId: string, changes: Array<FileChange>, status: ItemStatus, } | { "type": "image", itemId: string, status: ItemStatus, path: string | null, prompt: string | null, } | { "type": "usage", 
/**
 * Totals for the session so far.
 */
total: TokenUsage, } | { "type": "contextSize", usedTokens: number, windowTokens: number | null, } | { "type": "rateLimits", quota: QuotaSnapshot, } | { "type": "approvalRequested", request: ApprovalRequest, } | { "type": "approvalResolved", id: string, decision: ApprovalDecision, decidedBy: Decider, } | { "type": "turnCompleted", turnId: string | null, status: TurnStatus, durationMs: number | null, usage: TokenUsage | null, } | { "type": "error", error: ProviderError, } | { "type": "notice", level: NoticeLevel, message: string, } | { "type": "exited", code: number | null, 
/**
 * The last lines it wrote to stderr, when it failed.
 */
stderrTail: string | null, };
