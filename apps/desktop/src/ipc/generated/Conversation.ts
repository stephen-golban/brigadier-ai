// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ConversationId } from "./ConversationId";
import type { ConversationKind } from "./ConversationKind";
import type { Lifecycle } from "./Lifecycle";
import type { ProjectId } from "./ProjectId";
import type { Setup } from "./Setup";

/**
 * A session (inside a project) or a chat (outside any project), as listed in the sidebar.
 */
export type Conversation = { id: ConversationId, kind: ConversationKind, 
/**
 * Set for sessions, absent for chats.
 */
projectId: ProjectId | null, title: string, 
/**
 * When it was pinned; absent when not pinned.
 */
pinnedAtMs: number | null, createdAtMs: number, 
/**
 * Last activity (creation, rename, pin, message).
 */
updatedAtMs: number, 
/**
 * Absent for conversations created before setups existed (they get one on first use).
 */
setup: Setup | null, lifecycle: Lifecycle, };
