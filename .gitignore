# Nothing is built here yet; these are the things probes and future work leave behind.
node_modules/
dist/
.tmp/
*.log

# Probe output — evidence belongs in a ticket comment, not in the tree.
probes/out/

# OS noise
.DS_Store
