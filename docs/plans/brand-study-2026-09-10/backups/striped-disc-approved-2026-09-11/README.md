# Approved striped-disc intro backup

Saved at the user's request before changing headline spacing or exploring alternate copy on 2026-09-11.

Open `index.html` through the local brand-study server. The compiled intro, stylesheet, Arrival audio and logo are stored here with relative URLs, so the backup does not depend on the current production assets. `source/` preserves the implementation and `manifest.json` records original source SHA-256 hashes. Source snapshots are byte-identical; only compiled asset URLs were made portable.

This preserves the approved seven-second stripe-gathering sequence and the original “Your next idea starts here.” typography, including the earlier tight letter spacing. Converge remains a separate favorite.
