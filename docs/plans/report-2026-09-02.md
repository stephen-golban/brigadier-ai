# Report, autonomous run 2026-09-02 (12:29–13:27 EEST)

Lead session working `docs/plans/autonomous-run-2026-09-02.md` while the owner was away. Nobody
clicked anything in a Tauri window; every "works" below was proven by an `#[ignore]` supervisor
test against a real `claude` 2.1.258 child on `claude-haiku-4-5`, or by unit tests, or by gates.

## Per item

| item | state | commit | live cost (store `cost_usd_cumulative`) |
|---|---|---|---|
| 1 approvals end to end | done | `f1b911f` | $0.045220 (re-run 12:39), $0.046022 (final re-run 13:09) |
| 2 resume | done | `b2c1a4c` | $0.030894 (final, cumulative across both children) |
| 3 worktree per session | done | `b2c1a4c` (same commit as 2, see decisions) | $0.029651 (lead run) |
| 4 two live sessions on one project | done | `8351335` | $0.026623 + $0.026597 = $0.053220 (final) |
| 5 UI restyle | done | `41e030f` (shell) + `b2c1a4c` (Resume/branch/cleanup UI) | none |
| 6 carry-overs | done: data-dir lock, batcher maps; not started: folder picker | `7f03eb5` | none |

### 1. Approvals (`f1b911f`)
- Rust diff lead-reviewed in full; clean. Ceiling in `live_approvals.rs` raised $0.05 → $0.08 (the
  morning run cost $0.0459).
- Re-run 12:39: pass, $0.045220; final re-run 13:09 after the worktree change: pass, $0.046022.
  The test's throwaway repo now needs an initial commit (item 3 refuses an unborn HEAD); fixed in
  all three live tests. The denied-`Write` check now also looks in the session's worktree cwd.
- Not checked: `pending_approvals` across a webview reload (no window).

### 2. Resume (`b2c1a4c`)
- Same `session_id` row; adapter `seq` seeded from `sessions.last_event_seq`; `Op::SessionResumed`
  clears `ended_at`/`exit_code`; `resume_session` command; `not_resumable`; Resume button.
- Blind adversarial review found a **real blocker**: two concurrent `resume_session` calls both
  passed the `is_live` check and spawned two children on one transcript (proved with
  `tokio::join!`). Fixed with a reservation under the `live` mutex before any await plus a
  generation-guarded `consume`; regression test `two_concurrent_resumes_cannot_both_win`.
- Cost/usage now accumulate across a resume (`cost_and_usage_accumulate_across_a_resume`); the
  raw log keeps the CLI's own numbers. Three earlier live rows ($0.003187, $0.003264, $0.003210)
  were written before this fix and under-report by the first child's spend.
- Final live proof: recall answer `pelican`, `old_seq=8`, first new seq 10, rows 7 → 13, row
  cost $0.030894.
- Contradictions with `docs/research/resume.md`: no per-child `instance_id` exists (it names the
  driver); `session-started` does not arrive on a bare resume until the first turn, so a resumed
  row sits in `starting` with a live composer; `session_not_found_upstream` is reserved, not
  emitted (stderr is drained to tracing). `started_at` moves to the resume time on the first turn.
- Not checked: `--fork-session`, resume of a swept transcript, a live crash-then-resume of a
  `failed` row, the resume-from-summary dialog.

### 3. Worktree per session (`b2c1a4c`)
- Branch `brigadier/<8 hex>`, path `<project>/.brigadier/worktrees/<id>`, `.brigadier/` in
  `$GIT_COMMON_DIR/info/exclude` at project-add; unborn HEAD refused; branch rolled back on a
  failed `add` and on a driver-start failure; explicit `cleanup_worktree(session_id, force)`,
  branch always kept; `git worktree prune` per project at app start; `SessionView.worktree_path`
  and `branch`; sidebar branch chip; cleanup flow in the composer.
- **Deviation from the plan:** the short id is not the session UUID's first 8 hex — the driver
  mints the session id after it is handed the cwd. The supervisor mints its own UUID.
- **Deviation from the plan:** cleanup is explicit, never automatic on end/kill, because a
  resumed session needs its worktree as cwd.
- Blind adversarial review found two **blockers** (measured on git 2.50.1): `git status
  --porcelain` without `--ignored` misses gitignored files and, under `status.showUntrackedFiles=no`,
  untracked files, while `git worktree remove` without `--force` deletes them; an unforced cleanup
  could destroy `node_modules`/`.env` while reporting nothing touched. Fixed (`--ignored=matching --untracked-files=all`, `-c status.showUntrackedFiles=normal`, `LC_ALL=C`, `-d` instead of `-D` on rollback, cleanup refused while a resume is reserved, sub-directory project roots refused, the driver-failure rollback now has a test); 12 worktree unit tests; live re-run after the fixes passed at $0.029848.
- Live proof: resume from a worktree cwd **works** (measured; same provider session id across the
  resume); cleanup removed the checkout, branch survived; $0.029651.
- Not checked: recall across a worktree resume (the prompt did not test memory), submodules,
  two brigadier processes writing one `info/exclude`, non-English git locale on a gettext build.

### 4. Two live sessions (`8351335`)
- `live_two_sessions.rs`: both children live together (max concurrent 2, observed per poll),
  interleaved feed with `t` non-decreasing in every batch and per-session `q` strictly increasing,
  counters 7/7 rows, 0 dropped, `ALPHA`/`BRAVO` each only in its own session, `shutdown_with(10s)`
  in 581 ms, both `Exited(0)`.
- Not checked: more than two sessions, an approval mid-flight with both live, row-cap overflow.

### 5. UI restyle (`41e030f`, then the wiring in `b2c1a4c`)
- **Source: a screenshot** of `/Applications/ChatGPT.app` 26.825.51511 (it opened in its Codex
  view), taken with `screencapture -l`; only observed, never signed in or typed into, quit after.
  Tokens sampled from the PNG: sidebar 276 px `#3a3b3b`, thread `#181818` centred on 736 px,
  composer radius 20 px, system font. It is dark-only; no light scheme shipped.
- `docs/plans/ui-2026-09-02.png` shows the built app; a worker navigated it via the accessibility
  API (sidebar buttons only, nothing typed, no session started). Method and departures in
  `docs/plans/ui-restyle-notes.md`.
- Not checked: the feed gate after the restyle (row markup unchanged; no burn re-run), a populated
  approvals dock, the dev burn panel, sidebar vibrancy and hidden title bar (need `src-tauri`).

### 6. Carry-overs (`7f03eb5`)
- Data dir locked against a second app instance: `Store::open` takes an exclusive `flock` on
  `<data_dir>/brigadier.lock` through `std::fs::File::try_lock` (stable since Rust 1.89; toolchain
  here 1.98; `docs/research/data-dir-lock.md`) before it settles stale sessions; a second open
  returns `data_dir_locked`. Unit test in-process; cross-process not tested.
- Batcher forgets a project once every session it saw start has exited and its buffers are
  empty; caps untouched. Sessions the batcher never saw start keep their counters (the replay
  driver emits no `SessionStarted`, and the ten-session load test reads counters after the end).
  Consequence to know: a resumed session's `rows_total` restarts from zero after its earlier
  lifetime was pruned.
- Not started: `tauri-plugin-dialog` folder picker; the burn profiling (excluded by the plan).
- No adversarial review for this item: small, self-contained, lead-reviewed in full.
- Flake seen once by the worker in an untouched crate: `crates/proc/tests/orphans.rs`
  `a_kill_takes_down_the_whole_group` ("group still has members"); passed on rerun and in my
  three full gate runs. Not investigated.

## Live spend, this session (store rows, every run)

| # | run | cost |
|---|---|---|
| 1 | live_approvals re-run | $0.045220 |
| 2 | live_resume, item 2 worker | $0.003187 (under-reports, pre-fix) |
| 3 | live_resume, lead | $0.003264 (under-reports, pre-fix) |
| 4 | live_worktree, item 3 worker | $0.003210 (under-reports, pre-fix) |
| 5 | live_worktree, lead | $0.029651 |
| 6 | live_two_sessions, item 4 worker | $0.052321 |
| 7 | live_resume, lead, after fixes | $0 (failed before spawn: unborn HEAD) |
| 8 | live_resume, final | $0.030894 |
| 9 | live_approvals, final | $0.046022 |
| 10 | live_two_sessions, final | $0.053220 |
| 11 | live_worktree, final after the review fixes | $0.029848 |
| | **store-reported total** | **$0.296837** |

Runs 2–4 predate the cost-accumulation fix and omit their first child's spend (about $0.027 each
by comparison with run 8), so the true total is about $0.38. Cap was $0.60. Only `claude-haiku-4-5`.

## Gates at final code HEAD (`7f03eb5`; the docs commit follows it)
- `cargo test --workspace`: 209 passed, 0 failed, 5 ignored (the five live tests). Exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings`: exit 0.
- `cargo doc --workspace --no-deps`: 0 warnings, exit 0.
- `npx tsc --noEmit`: exit 0. `npm run build`: exit 0, ~258 kB JS.
- `npm run tauri build`: exit 0 at `8351335` and `7f03eb5`; `target/release/bundle/macos/brigadier.app`.
- Live proofs at or after `b2c1a4c`: `live_resume`, `live_approvals`, `live_two_sessions`,
  `live_worktree` all exit 0 (costs in the spend table). Not rerun after `7f03eb5`.

## Decisions made in the owner's absence
- Ceiling in `live_approvals.rs` $0.05 → $0.08.
- Items 2 and 3 share one commit: they were built in parallel in the same files
  (`crates/supervisor/src/lib.rs`, `error.rs`, `src-tauri/`, the contract) and a hunk-level split
  was not worth the risk in the time left. Item 5's shell landed before their UI wiring.
- Worktree short id minted by the supervisor, not derived from the session UUID (see 3).
- Worktree cleanup explicit, never on end/kill; branch never deleted by any cleanup path.
- A project root that is a subdirectory of a repository is refused as a worktree base (review
  finding, measured: the checkout would be the whole repo with the cwd two levels off).
- Permission mode on resume is `default`; the UI says so.
- Research contradicted: `resume.md` §7 "new `instance_id`" (none exists); `worktree-git.md` §7
  `check-ref-format --branch` exits 128, not 1; `--git-common-dir` is relative to the `-C` dir.
