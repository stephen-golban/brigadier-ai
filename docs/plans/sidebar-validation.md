# Sidebar rebuild validation — 2026-09-08

The latest sidebar instructions supersede the earlier branding and motion requirements: use a text-only Brigadier wordmark, an icon-only header Search with a glass keyboard-shortcut tooltip, and New chat followed by Notes; remove Recents and the footer Notes action; keep hover-only project/session actions, blur, and smooth sidebar/folder layout changes.

| Check | Result |
|---|---|
| HeroUI/NextUI source and package references | PASS — zero matches |
| Raw Tailwind palette utilities | PASS — zero matches |
| Hex colors in component files | PASS — zero matches |
| Sidebar width and right edge | PASS — browser measured 264px and 1px white at 8% |
| Neutral selected rows | PASS — semantic white-at-8% fill; no colored project selection |
| Sidebar typography | PASS — 13px navigation; 17px text-only wordmark; 14px workspace title |
| Clickable navigation rows | PASS — measured 32px high, 10px radius |
| Icon and label alignment | PASS — 16px icons at x=12; project and session labels at x=36 |
| Internal sidebar dividers | PASS — grouping uses whitespace |
| Warning color on fills/borders | PASS — no warning/status fills or borders in sidebar controls |
| Webfonts | PASS — zero font-face, Google Fonts, or WOFF references |
| Hover behavior | PASS — row backgrounds and explicitly requested icon color changes only; no lifts, shadows, or borders |
| Smooth layout | PASS — 220ms sidebar width transition pushes main area between x=0 and x=264; 200ms intrinsic folder-height transition; reduced-motion override |
| Project menu activation | PASS — hover does not open it; click/keyboard activation opens it; Escape restores trigger focus |
| Blur | PASS — reusable 24px backdrop blur menu; Tauri Sidebar material compiled and observed in isolated macOS app; translucent browser fallback with a muted blue/teal backdrop |
| Back/forward navigation | PASS — workspace history and unavailable-location tests |
| Session/project behavior | PASS — selection, rename, tags, new chat, pin, search, notes, and guarded Trash tests retained |
| Archive | PASS — hide/restore tests pass; device-local persisted sidebar preference, independent of process lifecycle and Trash |
| Build and tests | PASS — production frontend build; 298 tests in 32 files; isolated Tauri development build |

Native appearance uses the existing transparent macOS window and Tauri's built-in `setEffects` API, with the `allow-set-effects` capability and dark native theme. No Rust code or IPC signatures changed. Native QA used a separate application identifier and data directory. The installed application must be rebuilt/relaunched to pick up its capability/config changes.

The project menu retains Rename, New chat, Reveal in Finder where available, all existing tag choices in a submenu, and Move to Trash. Session rows expose Pin and Archive directly; Archived chats in the account menu provides Restore and the existing guarded Trash action.

The final full test suite passed with `--maxWorkers=2` after a concurrent build/preview run produced timing failures. The production build retains its existing large-chunk warning.

The header refinement passed the 12 existing sidebar tests and 2 new tooltip interaction tests. The Search tooltip appears on hover/focus, shows the platform shortcut, supports Escape dismissal, and preserves search activation. Browser inspection confirmed Notes below the branding, no Recents, and only the Settings action icon beside the account in the footer.

The final header refinement uses a single 44px row: the toolbar overlays the sidebar column and aligns with the workspace title, eliminating the prior extra header row. The native control inset is 84px; the collapsed toolbar ends at 156px, with the title icon starting at 172px. Browser measurements confirmed header y=0/height=44, sidebar width=264 when pinned and 0 when collapsed, and sidebar content beginning at y=44. The same layout was visually checked in the isolated native QA app. Hover preview remains an overlay without pushing the main area; pinning animates the content width.

The supplied edit-svgrepo-com.svg (14px) replaces the earlier edit-3 icon, and notes-svgrepo-com.svg supplies the Notes row icon (16px). Search uses a 1.5px SVG stroke. Browser computed styles confirmed tooltip radius 10px, blur 24px, text 12px, and shortcut text 11px. Workspace tools and creation actions are retained in the title's click-activated glass menu. The existing sidebar, tooltip, hover-preview, attention, workspace, and navigation tests all pass.

The recording supplied at 01:55:35 was inspected through frames across its full 10-second duration. Navigation now stays fixed on the left while a synchronized 220ms clip and content-width transition reveals/covers it. Hover preview retains its overlay behavior. Notes is now Notepad, uses the supplied note-svgrepo-com.svg, and has a separate 32px plus button with a New note tooltip. The plus opens an unsaved draft, focuses its title, and retains existing save/discard behavior. A regression test covers direct draft creation, saving, and returning to ordinary Notepad browsing.

Tooltip corners were reduced to 10px after the 16px version appeared pill-shaped. New chat now displays and handles ⌘N on macOS / Ctrl+N elsewhere, including when the sidebar is collapsed; modal dialogs and repeated key events do not create additional chats. All 24 sidebar, tooltip, shortcut, and vibrancy checks pass. Browser inspection confirmed a 28px sidebar backdrop blur and 42%-opaque tint over a muted blue/teal background; native macOS continues to reveal real behind-window Sidebar material. Browser CSS cannot blur the desktop outside its webview.

The supplied search-svgrepo-com.svg now supplies all search glyphs, retaining its circle/handle paths and removing only the white artboard. Standard icon buttons share one CSS contract through the Button primitive: 28px square, 6px padding, 16px icon box, 10px radius, selected-token hover fill, pressed-token active/menu-open fill, and disabled foreground. Browser measurements confirmed identical metrics across 17 rendered toolbar/sidebar/message/composer controls; the open workspace menu trigger measured white-at-12% fill. Existing wrapper-free project/session actions explicitly retain the bare icon style. Production build and all 301 tests pass.

Installed release on 2026-09-08 at 02:21: the supplied sidebar toggle and closed/open folder SVG paths are shared throughout navigation and workspace controls. The second folder SVG is treated as the open state. `tauri build --bundles app` completed, all 301 tests passed, and the locally ad-hoc-signed bundle passed strict signature verification. `/Applications/Brigadier.app` was replaced and launched successfully; the temporary app backup was subsequently deleted at the user’s request; do not create further app backups. Existing user data was retained.

The subsequent native comparison refinement uses a 46px header, a 90px native toolbar inset, and 8px gaps around 24px icon controls (16px glyphs, 4px padding, 8px corners). Sidebar navigation rows are 28px high with 8px corners, and the branding row is 32px high. The supplied bell is rendered in the same 16px box as Search. Native traffic-light configuration is x=16/y=23. The rebuilt app was installed and reopened, with the existing draft retained and no backup created. The release build, strict signature verification, and all 301 tests passed.

The latest refinement restores Search to a uniform 1.5px stroke and uses the newly supplied even-odd closed-folder SVG. Sidebar rows and footer have an 8px outer inset; branding, section headings, and project icons align at 16px, with project/session labels at 40px. Project, workspace, and session ellipsis menus now use the native macOS menu API, deriving actions, separators, checked states, and nested menus from the existing menu definitions. Browsers, other platforms, unsupported contents, and native API failures retain the current glass menu. Native cancellation releases the menu resource without opening the fallback.

All 307 tests in 33 files pass, including native selection, checked submenus, keyboard activation, cancellation, cleanup, and fallback behavior. The release build and strict ad-hoc signature verification passed. `/Applications/Brigadier.app` was replaced and launched without a backup. Native accessibility inspection confirmed an actual macOS menu with the existing project actions and tag submenu; selecting Rename opened the existing project-name field, then Cancel preserved its name. The existing composer draft was retained.

Project menus now expose Pin/Unpin, Edit (gear), and Remove project (cross), with the existing guarded Trash operation retained. Project pins persist locally and sort pinned projects first without changing the source project list. These three actions share their SVGs with native macOS menu icons; images are released after dismissal or failure. All horizontal ellipsis controls, including the composer, use the supplied three-point path. The New chat row displays its platform keyboard badge alongside its existing shortcut and tooltip. The browser preview was visually checked with the menu open. All 309 tests pass; the release was built, signature-verified, installed without a backup, and opened successfully. Native accessibility inspection confirmed the updated macOS menu labels; the existing draft was retained.

The continuation refinement replaces all pin glyphs with the user's supplied outline path and uses its filled shape for Unpin/pinned indicators. The uploaded archive SVG now supplies archive glyphs. Shared glass tooltips cover Pin, Unpin, and Archive session actions, with project-menu pin hints in the web fallback. Browser measurements confirmed 20px below the branding row, 2px option/session gaps and project-to-session inset, a 1px hairline footer border, 8px account horizontal padding, an 18px avatar, 14px account text, and a 14px tertiary Projects heading with normal letter spacing. The Projects heading is a keyboard-accessible disclosure with a hover/focus chevron and a persisted section state; the existing 200ms intrinsic-height animation preserves mounted folder/session state while collapsing.

All 311 tests pass, including section collapse/persistence and Pin/Unpin/Archive tooltip behavior. The release build and strict ad-hoc signature verification passed. The installed app was replaced without a backup, relaunched with its existing draft intact, and verified for Projects collapse/expand and native menu opening. The live browser preview remains available.

The next refinement rounds the hover-preview toolbar's top-right corner and navigation's bottom-right corner to 16px. Double-click/inline project renaming is removed; the three-dot Edit action now opens a 460px Edit project modal with the name field, Close, Remove local project, Cancel, and Save. The user explicitly excluded the Source folders section. Rename failures remain in the modal, blank names cannot submit, Cancel discards the draft name, and removal retains the existing guarded Trash flow.

Pinned and Projects now use the same section-heading component, typography, hover/focus chevron, and 200ms rotation. Pinned has its own persisted disclosure state and intrinsic-height reveal; browser measurements confirmed Projects moves from y=264 to y=236 as the 28px pinned row collapses. Session pin/archive glyphs measure 14px in both sections and retain hover/focus-only actions; the persistent pinned-session glyph was removed. All 313 tests pass. The release was built, strictly signature-verified, and installed without a backup. Native menu Edit was verified to open the new modal; Cancel preserved the project name and existing composer draft. The browser preview remains open.

The status refinement replaces the ellipsis placeholder with an 11px animated ring and uses an 8px blue attention dot. Status and archive slots share the same right alignment; hover/focus hides status and exposes Pin/Unpin and Archive actions. Truncated session titles open a right-side glass tooltip with the full title and project name; fitting titles do not open it. Pinned labels align with section headings. The account trigger now fits its avatar/name rather than filling the footer: live browser measurements confirmed equal 8px left/right padding and a 95.8px trigger width for Stephen.

All 315 tests in 34 files pass, including status precedence and overflow-only keyboard-accessible tooltips; TypeScript and the release build pass. The preview server was restarted and the refreshed preview shown. The release was strictly signature-verified, installed without a backup, and launched successfully.

Search now uses the supplied shadcn command-palette composition adapted to the existing native controls and tokens: a 512px dialog, input with the supplied Search glyph, grouped navigation/projects/sessions/notes, separators, selected rows, empty results, and keyboard navigation that keeps focus in the combobox. Existing custom note, folder, and edit glyphs are reused. New chat and Notepad text/icons resolve to the primary text color; the New chat shortcut remains in its tooltip and is removed from the sidebar row.

Notepad opens in the main content area with a centered 720px column, heading/description, search, note list, and a top-right Create action, following the Scheduled page reference. Note editing, preview, saving, prompt inclusion, and guarded Trash are retained. Unsaved-note confirmation covers sidebar and Back/Forward navigation. The conversation/composer remain mounted while hidden. Browser inspection verified zero composer height in Notepad and restored height on Back, plus working Forward navigation. All 317 tests in 35 files pass; the release build includes TypeScript validation.

The release was signature-verified and installed without a backup. After relaunching the updated process, native accessibility inspection confirmed the full Notepad page (Create, heading, Search notes, and the existing Test note), with the sidebar still visible. The live browser preview is left on Notepad.

The active session header now has Rename, Pin/Unpin, Archive/Unarchive, Fork, and the existing Session history action, using the shared native macOS menu and glass fallback. Rename persists a local display-name override and updates all peer-title consumers; archive state is shared between the header and sidebar. The toolbar keeps a constant bottom-border width in open, closed, and hover-preview states. Browser measurements confirmed the toggle stays at y=9.75px when open and closed.

The user explicitly authorized backend changes for Fork and excluded scheduled tasks. Fork uses Claude’s documented `--resume=<token> --fork-session` flags (https://code.claude.com/docs/en/cli-usage), creates a fresh harness/provider conversation, preserves inherited display history, starts idle, and prepares an independently owned Git worktree seeded from the source working files. The source stays unchanged; a refused spawn rolls back its new worktree. Native peers record the source relationship. The browser mock forks its conversation without sending work.

Validation: all 319 frontend tests and 348 core/store/supervisor unit tests pass. The live Claude Haiku fork test verified recalled context, distinct provider IDs, and an unchanged parent; the two short turns cost $0.02561. Browser activation selected the fork and displayed inherited history with zero new usage.

The release build passed, was ad-hoc signed and strictly verified, and replaced the installed app without creating a backup. After relaunch, native accessibility inspection verified Rename, Pin, Archive, Fork, and Session history in the session menu. The live browser preview remains available.
